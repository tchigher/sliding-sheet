name: sliding_sheet
description: A widget that can be dragged and scrolled in a single gesture and snapped to a list of extents.
version: 0.0.1
author: Bendix Mahnke <bendixq.mahnke@gmail.com>
homepage: https://github.com/BendixMa/sliding-sheet

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: